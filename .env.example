# Copy this file to `.env` (in the repo root) and fill in your real key.
# `.env` itself is gitignored — it will never be committed.
#
# Get a key from https://the-odds-api.com/ (free tier available).
ODDS_API_KEY=
