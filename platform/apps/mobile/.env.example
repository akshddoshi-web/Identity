# Copy to .env. Points at the web app (apps/web), which hosts the shared API.
# On a physical device/simulator, "localhost" means the device itself — use
# your computer's LAN IP instead, e.g. http://192.168.1.23:3000
EXPO_PUBLIC_API_URL="http://localhost:3000"
