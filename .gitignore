__pycache__/
*.pyc
.venv/
venv/
data/*.db
data/*.sqlite
# Exception: a committed, deploy-time seed DB (real NFL+NCAAF history, no
# logged predictions) — see data_pipeline/db.py::ensure_seeded() and
# README's "Deploying to Streamlit Community Cloud" section for why this
# one file is intentionally tracked while every other .db is not.
!data/seed_betting.db
data/raw/
data/cache/
*.log
.env
.DS_Store
.pytest_cache/
*.egg-info/
