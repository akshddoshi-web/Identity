numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.1
streamlit>=1.31
plotly>=5.18
requests>=2.31
PyYAML>=6.0
python-dateutil>=2.8
pytest>=7.4
pyarrow>=14.0
python-dotenv>=1.0

# Optional, sport-specific real-data ingestion clients — intentionally NOT
# installed by default: nfl_data_py pins pandas<2.0 and nba_api pins
# numpy<2.0, both of which conflict with this repo's pandas>=2.1/numpy>=1.26
# (installing them WILL silently downgrade pandas and corrupt the rolling-
# feature engineering in models/features.py — this bit us once already).
# data_pipeline/sources/nfl_nflverse.py fetches nflverse's parquet releases
# directly over HTTP and does not need nfl_data_py at all. NBA ingestion
# (data_pipeline/sources/nba_nba_api.py) does need nba_api; install it in an
# ISOLATED virtualenv, never into this project's main environment:
#   python -m venv .venv-nba && .venv-nba/bin/pip install nba_api
#   .venv-nba/bin/python scripts/ingest_real_data.py --sport NBA
# nba_api==1.5.*
